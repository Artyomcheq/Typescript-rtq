export interface IPost {
    id: number;
    title: string;
    body: string;
}

export interface IAlbum {
    id: number;
    title: string; 
}

export interface ITodos {
    id: number;
    title: string;
    completed: boolean;
}